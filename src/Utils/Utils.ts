class Utils
{
    public static CreateLabel():egret.TextField
    {
        var label:egret.BitmapText = new egret.BitmapText();
        var lable:egret.TextField = new egret.TextField();

        return null;
    }
}